serve:
	bundle exec jekyll serve

serve-with-dafts:
	bundle exec jekyll serve --dafts

install-deps:
	bundle install
	# Once complete, this should install jekyll and everything else required to run the site	